<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android">

    <!--普通权限，无需手动申请-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!--音频可视化必需，Visualizer 依赖此权限，否则部分机型返回全 0-->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--危险权限，需要手动申请-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/launcher_logo"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.BigImageActivity" />
        <activity android:name=".activity.DragMapActivity" />
        <activity android:name=".activity.TimeLineActivity" />
        <activity android:name=".activity.RadarScanActivity" />
        <activity android:name=".activity.CompassActivity" />
        <activity android:name=".activity.SatelliteStatusActivity" />
        <activity android:name=".activity.PtzActivity" />

        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="56fa2f20eef95e6107f998a5b59f0e10" />
    </application>
</manifest>